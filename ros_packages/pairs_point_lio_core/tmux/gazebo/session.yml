# do not modify these
root: ./
name: simulation
socket_name: pairs
attach: false
tmux_options: -f /etc/ctu-pairs/tmux.conf
# you can modify these
pre_window:
  - export UAV_NAME=uav1
  - export RUN_TYPE=simulation
  - export UAV_TYPE=x500
  - export PLATFORM_CONFIG=`ros2 pkg prefix pairs_uav_gazebo_simulator`/share/pairs_uav_gazebo_simulator/config/pairs_uav_system/$UAV_TYPE.yaml
  - export WORLD_FILE=`ros2 pkg prefix pairs_gazebo_common_resources`/share/pairs_gazebo_common_resources/worlds/pine_forest.sdf
  - export RMW_IMPLEMENTATION=rmw_zenoh_cpp
  - export USE_SIM_TIME="true"
  - export LIBGL_ALWAYS_SOFTWARE=false
  - export MESA_D3D12_DEFAULT_ADAPTER_NAME=NVIDIA
startup_window: status
windows:
  - zenoh_router:
      layout: tiled
      panes:
        - ros2 run rmw_zenoh_cpp rmw_zenohd
  - gazebo:
      layout: tiled
      panes:
        - ros2 launch pairs_uav_gazebo_simulator simulation.launch.py gz_headless:=false gz_verbose:=true world_file:=$WORLD_FILE
        - |
          waitForGazebo; sleep 5; ros2 service call /pairs_drone_spawner/spawn pairs_msgs/srv/String "{value: '1 --$UAV_TYPE --enable-rangefinder --enable-ouster model:=OS0-64 use_gpu:=True horizontal_samples:=128 update_rate:=10 --pos 0 0 0.5 0'}"
  - px4_api:
      layout: tiled
      panes:
        - ros2 launch pairs_uav_px4_api api.launch.py
  - core:
      layout: tiled
      panes:
        - ros2 launch pairs_uav_core core.launch.py
          platform_config:=$PLATFORM_CONFIG
          world_config:=./config/world_config.yaml
          custom_config:=./config/custom_config.yaml
          network_config:=./config/network_config.yaml
  - point_lio:
      layout: tiled
      panes:
        - ros2 launch point_lio point_lio.launch.py preset:=simulation_gazebo topic_imu:=hw_api/imu topic_pc:=ouster/points custom_config:=./config/point_lio.yaml
  - status:
      layout: tiled
      panes:
        - ros2 run pairs_uav_status status.sh
  - mapping:
      layout: tiled
      panes:
        - ros2 launch pairs_octomap_server octomap.launch.py custom_config:=./config/octomap_server.yaml lidar_3d_0:=ouster/points
  # - static_tfs:
  #     layout: tiled
  #     panes:
  #       - ./config/static_tfs.sh
  - autostart:
      layout: tiled
      panes:
        - ros2 launch pairs_uav_autostart automatic_start.launch.py custom_config:=./config/automatic_start.yaml
  - takeoff:
      layout: tiled
      panes:
        - waitForCore; ./takeoff.sh
  - rviz:
      layout: tiled
      panes:
        - waitForCore; ros2 run rviz2 rviz2 -d ./rviz.rviz --ros-args -p use_sim_time:=true
  - layout:
      layout: tiled
      panes:
        - waitForCore; sleep 2; ~/.i3/layout_manager.sh ./layout.json
